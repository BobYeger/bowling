// Artifact-build stand-in for babylon.js: the page loads babylon.js + babylonjs.loaders from the
// CDN as classic scripts (injected by vite.config.js), which define window.BABYLON.
export default window.BABYLON;
