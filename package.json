{
  "name": "marker-games",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:artifacts": "node scripts/build-artifact.mjs",
    "test": "playwright test",
    "render:characters": "node scripts/render-characters.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^9.25.0",
    "@babylonjs/loaders": "^9.25.0",
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^6.0.0"
  }
}
